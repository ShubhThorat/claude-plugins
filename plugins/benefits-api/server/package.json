{
  "name": "benefits-api-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server for employee benefits data via your HTTP API.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "esbuild index.js --bundle --platform=node --format=esm --outfile=bundle.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
